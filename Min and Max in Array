class Solution:
    def getMinMax(self, arr):
   
        mn = arr[0]
        mx = arr[0]

        for x in arr:
            if x < mn:
                mn = x
            if x > mx:
                mx = x

        return [mn, mx]   
